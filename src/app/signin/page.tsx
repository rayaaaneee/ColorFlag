import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Sign In",
}

const SignIn = () => {
    return (
        <div>
        <h1>Sign In</h1>
        </div>
    );
}

export default SignIn;