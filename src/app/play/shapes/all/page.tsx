import { HeadingOne } from "@/components/utils/headings";

export interface PageProps {
    // empty
}

const Page = ({}: PageProps) => {

    

    return (
        <div>
            <HeadingOne>
                Guess the shape of
            </HeadingOne> 
        </div>
    );
}

export default Page;